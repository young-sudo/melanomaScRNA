params {
    outdir = "results"
}

process {
    cpus = 2
    memory = "4 GB"
}

profiles {
    conda {
        conda = "envs/r-env.yml"
    }

    docker {
        container = "yourdocker/r-melanoma-pipeline:latest"
    }
}
